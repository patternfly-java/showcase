{
  "name": "patternfly-showcase",
  "description": "Showcase for PatternFly Java",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --outDir target/gwt/devmode/war/showcase --emptyOutDir false --base /showcase --sourcemap true --minify false",
    "prod": "vite build --outDir target/vite --base /showcase"
  },
  "devDependencies": {
    "vite": "^4.4.11"
  },
  "dependencies": {
    "@github/relative-time-element": "^4.3.0",
    "@patternfly/patternfly": "^5.1.0",
    "@popperjs/core": "^2.11.8"
  }
}
